spring:
  cloud:
    discovery:
      enabled: false

eureka:
  client:
    enabled: false

app:
  jwt:
    secret: test-secret-key-for-cloud-back-server-jwt-hs512-minimum-length-64-chars-1234567890
  security:
    issuer: http://localhost:9000
  cors:
    allowed-origins: http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003,http://localhost:3005

stock:
  batch:
    internal:
      token: test-stock-batch-internal-token
